# Headless answers for the "postgresql" test case.
# Prompt keys come from archetype.rhai and the composed org/project prompt
# components (kebab-case). Anything not listed here falls back to prompt
# defaults via `archetect render -D`.
author_full: "Test Author <test@example.com>"
org-name: "acme"
solution-name: "widgets"
prefix-name: "inventory"
suffix-name: "service"
persistence: "PostgreSQL"
